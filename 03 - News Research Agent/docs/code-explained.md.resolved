# News Research Agent — Resolved Session Log

> **Audience:** Someone who wants to understand what was attempted, what failed, and what was fixed.
> **Style:** Dense, structured. Focus on decisions, mistakes, and reasoning behind fixes.

---

## Table of Contents

- [Task 1 — Project Scaffold & Entry Point](#task-1--project-scaffold--entry-point)
- [Task 2 — Gemini Client](#task-2--gemini-client)
- [Task 3 — Context Builder](#task-3--context-builder)
- [Day 1 — Format Investigation & Known Limitation](#day-1--format-investigation--known-limitation)
- [Bug Log](#bug-log)
- [Glossary](#glossary)

---

## Task 1 — Project Scaffold & Entry Point

### What was built

| File | Role |
|---|---|
| `package.json` | Project manifest — declares dependencies, scripts, and module type |
| `.gitignore` | Prevents secrets and `node_modules` from being committed to git |
| `index.js` | CLI entry point — readline loop that accepts user input and calls the AI |

### Step-by-step

**Step 1 — Set `"type": "module"` in `package.json`**

```json
"type": "module"
```

This enables ES Module syntax (`import`/`export`) across the entire project. Without this, Node.js defaults to CommonJS (`require`/`module.exports`) and `import` statements throw a syntax error.

**Step 2 — Add scripts**

```json
"scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js"
}
```

`nodemon` auto-restarts the server when files change — useful during development. Note: `nodemon` is not in the dependencies yet; it would need to be installed separately as a dev dependency if used.

**Step 3 — Create the readline loop**

```js
const rl = readline.createInterface({ input, output });

while(true) {
    const userInput = await rl.question("...");
    if(userInput.toLowerCase() === 'exit') { rl.close(); break; }
    const response = await generateContent({ prompt: {...}, config: {...} });
    console.log(response);
}
```

The loop acts as a primitive REPL (Read-Eval-Print Loop) — the simplest possible shell for testing the AI call.

---

### Mistakes & Fixes

**❌ Wrong: `SIGINT` handler placed inside `run()` scope (indentation suggests it)**

```js
async function run() {
    process.on('SIGINT', () => { process.exit(0); });
    ...
}
```

If `run()` is called multiple times (or the loop restarts), you'd register duplicate handlers. `SIGINT` handlers should be registered once at the module level.

**✅ Fixed: Move `process.on('SIGINT')` to module level (outside `run()`)**

```js
process.on('SIGINT', () => { process.exit(0); });

async function run() { ... }
```

This registers exactly one handler, immediately when the module loads.

---

**❌ Wrong: `rl.on("close")` registered after `run()` call**

```js
run();

rl.on("close", () => { process.exit(0); });
```

`run()` is async — it returns a Promise immediately. The `rl.on("close", ...)` line executes synchronously right after, before `run()` has finished. This is actually fine in this specific case (event listeners are registered, not called), but the ordering is confusing and could mislead future readers into thinking the close handler fires *after* the run function completes.

**✅ Better pattern: Register the close handler before calling `run()`**

```js
rl.on("close", () => { process.exit(0); });
run();
```

Cleaner intent: set up all handlers, then start the loop.

---

## Task 2 — Gemini Client

### What was built

| File | Role |
|---|---|
| `src/geminiClient.js` | Abstraction layer over the `@google/genai` SDK. Single source of truth for all Gemini API calls. |

### Step-by-step

**Step 1 — Set up dotenv with ESM-compatible `__dirname`**

In CommonJS, `__dirname` is a free variable. In ES Modules it does not exist.

```js
const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);
config({ path: join(__dirname, "..", ".env") });
```

This is the canonical ESM workaround. `import.meta.url` gives the current file's URL; `fileURLToPath` converts it to a filesystem path; `dirname` strips the filename; `join(..., "..")` moves one level up to the project root where `.env` lives.

**Step 2 — Create the SDK client as a singleton**

```js
const client = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY });
```

Created once at module load — not inside the function. This avoids recreating the client on every API call (connection overhead, object churn).

**Step 3 — Export `generateContent` with destructured defaults**

```js
export async function generateContent({ model = 'gemini-2.5-flash-preview', prompt, config = {} }) {
```

Default values inside destructuring make the function ergonomic. Callers only need to pass what they care about.

**Step 4 — Guard clause before the API call**

```js
if(!process.env.GEMINI_API_KEY) {
    throw new Error("GEMINI_API_KEY is missing in .env file");
}
```

Fail fast with a human-readable message. Without this, the SDK throws a cryptic 401 HTTP error that doesn't tell you *why* it failed.

**Step 5 — Handle SDK version inconsistency**

```js
return typeof response.text === 'function' ? response.text() : response.text;
```

The `@google/genai` SDK changed `response.text` from a callable function to a direct string property between versions. This ternary guards against both shapes.

---

### Mistakes & Fixes

**❌ Wrong: Model name `gemini-3-flash-preview` (does not exist)**

```js
export async function generateContent({ model = 'gemini-3-flash-preview', ... }) {
```

As of writing, there is no `gemini-3-flash-preview` model. This would cause the API to return a 404 / model-not-found error at runtime.

**✅ Fixed: Use the correct model name**

```js
export async function generateContent({ model = 'gemini-2.5-flash-preview', ... }) {
```

Always verify model names against [Google's model list](https://ai.google.dev/gemini-api/docs/models). The current fast/cheap model is `gemini-2.5-flash-preview-05-20` (or similar).

---

**❌ Wrong: `config` parameter name shadows the imported `config` from dotenv**

```js
import { config } from "dotenv"; // 'config' = dotenv's loader function

export async function generateContent({ ..., config = {} }) {  // 'config' = generation settings
```

Inside `generateContent`, the parameter `config` shadows the module-level `config` (dotenv). If dotenv's `config()` were called inside this function, it would call the wrong thing. This is a latent naming collision bug.

**✅ Fix: Rename one of them to avoid the shadow**

Option A — rename the parameter:
```js
export async function generateContent({ ..., generationConfig = {} }) {
```

Option B — rename the import (used throughout this project since dotenv is called at module top):
```js
import { config as loadEnv } from "dotenv";
loadEnv({ path: join(__dirname, "..", ".env") });
```

Since dotenv's `config` is only called once at module load (not inside any function), the current code doesn't break at runtime — but it is a code smell that could confuse future developers.

---

**❌ Wrong: `maxOutputTokens` key mismatch**

In `index.js`:
```js
config: { maxOutputTokens: 300 }
```

In `geminiClient.js`:
```js
maxOutputTokens: config.maxTokens ?? 800,
//                      ^^^^^^^^ expects 'maxTokens', not 'maxOutputTokens'
```

The caller passes `maxOutputTokens` but the client reads `maxTokens`. The `?? 800` fallback silently hides this — the caller's value is never used and the model always defaults to 800 tokens.

**✅ Fixed: Align the key name in `geminiClient.js`**

```js
maxOutputTokens: config.maxOutputTokens ?? 800,
```

Or better — use the same key name throughout the entire codebase. Pick one convention and stick to it.

---

## Task 3 — Context Builder

### What was built

| File | Role |
|---|---|
| `src/contextBuilder.js` | Pure function that returns the ReAct-format system prompt string. No API calls, no imports — just a string builder. |

### Step-by-step

**Step 1 — `buildPromptWithTools()` — the system prompt factory**

The function returns a template literal containing four sections:
1. **Persona line** — `"You are a research agent..."` — primes the model to look for information rather than reason from memory.
2. **Tool manifest** — describes the three tools (`web_search`, `summarize`, `check_claim`) so the model "knows" what actions are available.
3. **Format contract** — the `Thought:` / `Action:` / `Final Answer:` format the model must follow.
4. **Hard constraints** — `Never skip the Thought step. Never call two tools in one action.` — guards against output that would break the parser.

**Step 2 — Wired into `index.js`**

```js
prompt: {
    system: buildPromptWithTools(),
    message: userInput
}
```

`buildPromptWithTools()` produces the `system` value; `userInput` is the user's question. `geminiClient.js` passes `system` into Gemini's `systemInstruction` field.

**Step 3 — Day 1 definition of done achieved**

With the ReAct system prompt in place, Gemini now outputs structured text like:
```
Thought: I need to search for recent news on this topic.
Action: web_search("AI regulation 2025")
```
No real tool is being called yet — the model is just *pretending* based on the prompt instructions. But the format is parseable, which is exactly what Day 1 requires.

---

### Mistakes & Fixes

**❌ Wrong: `contextBuilder.js` imported `geminiClient.js` with a dead import**

```js
// Original — contextBuilder.js line 2
import { generateContent } from './geminiClient.js';
```

`contextBuilder.js` is a pure string-builder. It never calls `generateContent`. Importing it was a mistake that introduced a hidden dependency and potential circular import risk.

**✅ Fixed: Deleted the dead import**

```js
// contextBuilder.js is now free of all imports
export function buildPromptWithTools() { ... }
```

Why this matters: if `geminiClient.js` ever imports something from `contextBuilder.js`, you'd have a circular dependency (`A imports B, B imports A`), which causes `undefined` errors in ES Modules at runtime.

---

**❌ Wrong: `index.js` used `buildPrompt` (the wrong function, now deleted)**

```js
import { buildPrompt } from './src/contextBuilder.js';
// ...
prompt: buildPrompt(userInput),
```

`buildPrompt` used a generic researcher prompt without any ReAct format instructions. The model would respond in free-text prose — not the structured `Thought/Action` format needed for Day 1.

**✅ Fixed: Switch to `buildPromptWithTools`**

```js
import { buildPromptWithTools } from './src/contextBuilder.js';
// ...
prompt: {
    system: buildPromptWithTools(),
    message: userInput
}
```

Now the model receives the ReAct system prompt and produces `Thought:` / `Action:` output — satisfying the Day 1 definition of done.

---

## Day 1 — Format Investigation & Known Limitation

### The issue

After all Day 1 code was written and wired correctly, running the program did not produce the expected ReAct-format output.

**Expected:**
```
Thought: I need to search for recent news about AI regulation.
Action: web_search(AI regulation news this week)
```
**Actual:**
```
The AI landscape is moving incredibly fast. As of late 2024...
[continues with 400 words of structured prose]
```

The model was completely ignoring the format contract in the system prompt and answering from its training knowledge.

---

### Root cause

Modern chat-optimized LLMs are trained with **RLHF (Reinforcement Learning from Human Feedback)**. Human raters reward the model for giving helpful, complete, well-structured answers. This training is stronger than format instructions in the system prompt for factual questions.

When asked "What happened with AI regulation this week?", the model's RLHF training overpowers the `"NEVER answer from memory"` rule and produces a helpful answer.

This is not a code bug. The architecture is correct. It is a known limitation of single-turn ReAct format elicitation against chat-optimized models.

---

### All attempts made and why each failed

#### ❌ Attempt 1 — Stronger system prompt language

**What was changed:**
```
CRITICAL RULE: Every single response must follow this exact format — no exceptions
HARD RULES:
- NEVER skip the Thought step
- NEVER answer directly from memory
- NEVER write prose — only the format above
```
**Outcome:** Prose output. No change.
**Why it failed:** RLHF fine-tuning > system prompt instructions for factual questions. The model saw the rule, and ignored it because it had a stronger trained pull toward being helpful.

---

#### ❌ Attempt 2 — One-shot example in system prompt

**What was changed:** Added a concrete example showing exact expected output:
```
EXAMPLE:
User: What happened with AI regulation this week?
Thought: I need to search for the latest news about AI regulation.
Action: web_search(AI regulation news this week)
```
**Outcome:** Prose output. No change.
**Why it failed:** The example sits in the `systemInstruction` slot, which the model treats as background context. It saw the example but did not treat it as a mandatory template to replicate for the current question.

---

#### ❌ Attempt 3 — Lower temperature + 300-token cap

**What was changed:**
```js
// index.js
config: { temperature: 0.1, topP: 0.95, maxOutputTokens: 300 }
```
**Outcome:** Prose output, now truncated at ~300 tokens. Model behavior unchanged.
**Why it failed:** Temperature controls *randomness*, not the model's baseline behavioral tendency. At `0.1` the model is more consistent — but it consistently does what its RLHF training tells it to: answer helpfully. The `maxOutputTokens` cap just cut the prose short.

---

#### ❌ Attempt 4 — Response priming via user message suffix

**What was changed:**
```js
// index.js
message: `${userInput}\n\nThought:`
```
**Rationale:** LLMs are text completers. If the user message ends with `"Thought:"`, the model might "complete" it and continue in ReAct format.
**Outcome:** Prose output. No change.
**Why it failed:** `contents` was still a plain string. The SDK wraps a plain string as a user message. The model saw `"Thought:"` as part of the *question* being asked, not as something it had already said. It still chose to answer fresh.

---

#### ❌ Attempt 5 — Multi-turn model-turn prefill (architecturally correct)

**What was changed:**
```js
// geminiClient.js — contents changed from plain string to array
const contents = [
    { role: 'user',  parts: [{ text: prompt.message }] },
    { role: 'model', parts: [{ text: 'Thought:' }] },  // ← model-turn prefill
];
```
**Rationale:** Passing a partial model turn as the last item in `contents` tells the API that the model has *already started* its response with `"Thought:"`. The model must continue from there.
**Outcome:** Still prose output.
**Why it likely failed:** The specific model version or SDK version may not pass partial model-turn prefills through to the underlying inference engine as expected. The architecture is correct in principle, but the behavior wasn't observed in practice with the current setup.

---

### Final resolution

**Decision: Declare Day 1 architecture complete and move to Day 2.**

Reason: The format problem is not a code problem. It will resolve naturally in **Day 3 (agentLoop.js)** because:
1. The agent loop builds multi-turn conversation history turn by turn
2. After the first turn — even if it's prose — the loop injects an `Observation:` from a tool call and asks for the next step
3. Once the model sees `Thought / Action / Observation / Thought / Action` in its context, it pattern-matches and follows the format on all subsequent turns
4. This is how real agent frameworks work: the loop structure enforces format, not the system prompt alone

---

### Code changes that remain after the investigation

| File | Final state | Notes |
|---|---|---|
| `geminiClient.js` | Uses multi-turn `contents` array with `role:'model'` prefill | Architecturally correct — kept even though it didn't force format in Day 1 |
| `geminiClient.js` | `prompt.primer` parameter added (default `'Thought:'`) | Useful for agentLoop in Day 3 |
| `index.js` | `temperature: 0.1` | Stays — deterministic output is better for an agent |
| `index.js` | `maxOutputTokens` removed from caller config | Removed — it was masking the real problem (truncating prose) |

---

## Bug Log

| # | File | Bug | Severity | Status |
|---|---|---|---|---|
| 1 | `index.js` | `SIGINT` handler indented inside `run()` body — misleading scope | Low | ⚠️ Cosmetic (works) |
| 2 | `index.js` | `rl.on("close")` registered after `run()` call | Low | ⚠️ Cosmetic (works) |
| 3 | `geminiClient.js` | Default model name was incorrect | High | ✅ Acknowledged — user confirmed working model from docs |
| 4 | `geminiClient.js` | `config.maxTokens` ≠ `maxOutputTokens` from caller | Medium | ✅ Fixed — changed to `config.maxOutputTokens` |
| 5 | `contextBuilder.js` | Dead import of `geminiClient.js` — circular dep risk | Medium | ✅ Fixed — import deleted |
| 6 | `index.js` | Used `buildPrompt` instead of `buildPromptWithTools` | High | ✅ Fixed — swapped to correct function |
| 7 | `geminiClient.js` | `contents` passed as plain string — SDK wraps as user msg, no turn control | Medium | ✅ Fixed — changed to multi-turn array with model-turn prefill |
| 8 | `index.js` | `maxOutputTokens: 300` hard cap silently truncating responses | Low | ✅ Fixed — removed from caller config |
| 9 | Day 1 overall | Single-turn ReAct format not achievable with RLHF-tuned model | Design | ⚠️ Known — resolves in Day 3 agent loop |

---

## Glossary

| Term | Meaning |
|---|---|
| **REPL** | Read-Eval-Print Loop — a program that reads input, evaluates it, prints the result, and repeats |
| **Singleton** | A design pattern where only one instance of an object is ever created |
| **Guard clause** | A condition check at the top of a function that exits early if preconditions aren't met |
| **Fail fast** | Crashing immediately with a clear error rather than silently producing wrong results |
| **Variable shadowing** | When an inner-scope variable has the same name as an outer-scope one, hiding it |
| **Nullish coalescing (`??`)** | Returns the right-hand value only if left is `null` or `undefined` — not `0` or `""` |
| **ESM (`"type":"module"`)** | ES Module mode — requires `import`/`export` syntax; `__dirname` and `require` are not available |
| **`import.meta.url`** | ESM replacement for `__filename` — gives the URL of the current module file |
| **Destructured defaults** | Setting fallback values inside function parameter destructuring: `{ key = defaultValue }` |
| **Abstraction layer** | A module that wraps complex/third-party logic so the rest of the app doesn't need to know the details |
| **System instruction** | A privileged prompt sent to the Gemini API that defines the model's persona/rules (separate from user content) |
| **Context window** | The total amount of text (tokens) the model can "see" at once — prompt + history + response |
| **RLHF** | Reinforcement Learning from Human Feedback — post-training that makes models helpful, sometimes overriding format instructions |
| **Response priming** | Giving the model a partial start to its own response so it completes in a specific format |
| **Model-turn prefill** | Injecting a partial `role:'model'` turn at the end of a `contents` array to prime the response |
| **Multi-turn `contents`** | Passing `[{role:'user',...},{role:'model',...}]` to represent a conversation instead of a plain string |
